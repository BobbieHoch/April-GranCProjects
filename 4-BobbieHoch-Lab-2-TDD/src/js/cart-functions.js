//Seven Functions and their tests
//Function One
function calculateChange(payment, total) {
  return payment - total;
}
//Function Two
function isSufficientPayment(payment, total) => payment >= total;


//Function Three
function calculateTotal(itemsArray)
    
//Function Four
function addItem(itemsArray, name, price) {
  
} ;


//Function Five
function removeItem( 

);


module.exports = { calculateChange, isSufficientPayment, calculateTotal, addItem, removeItem };
